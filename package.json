{
    "name": "Opentech",
    "version": "1.0.0",
    "description": "Build the static src.",
    "cacheDirectories": ["client/node_modules"],
    "scripts": {
        "postinstall": "cd opentech/static_src && yarn install && npm run build:prod"
    },
    "engines": {
        "node": "8.11.3",
        "yarn": "1.7.0"
    }
}
